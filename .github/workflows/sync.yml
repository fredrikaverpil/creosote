# Code generated by pocket. DO NOT EDIT.
# Run `./pok github-workflows -force` to regenerate.
# See: https://github.com/fredrikaverpil/pocket

name: sync

on:
  workflow_dispatch:
  schedule:
    - cron: "0 8 1 * *" # 1st of every month at 8am UTC

permissions:
  contents: write
  pull-requests: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache-dependency-path: '**/go.sum'

      - name: Update pocket
        run: ./pok -v update

      - name: Run pocket
        run: ./pok -v

      - name: Create pull request
        uses: peter-evans/create-pull-request@v7
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          branch: ci/pocket-sync
          commit-message: "ci(pocket): sync and update"
          title: "ci(pocket): sync and update"
          body: |
            This PR syncs the repository using [Pocket](https://github.com/fredrikaverpil/pocket).

            Changes may include:
            - Updated pocket version
            - Regenerated workflow files
            - Reformatted code
          committer: GitHub Bot <github-actions[bot]@users.noreply.github.com>
          author: GitHub Bot <github-actions[bot]@users.noreply.github.com>
