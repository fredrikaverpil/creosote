# Code generated by pocket. DO NOT EDIT.
# Run `./pok github-workflows` to regenerate.
# See: https://github.com/fredrikaverpil/pocket

name: self-update

on:
  schedule:
    - cron: '0 0 1 * *'
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  self-update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: .pocket/go.mod
          cache: false
      - name: Run self-update
        shell: bash
        run: ./pok self-update -force
      - name: Run pocket
        shell: bash
        run: ./pok
      - name: Create pull request
        uses: peter-evans/create-pull-request@v8
        with:
          commit-message: 'chore(pocket): update pocket'
          title: 'chore(pocket): update pocket'
          body: |
            Automated [Pocket](https://github.com/fredrikaverpil/pocket) self-update.

            Run `./pok self-update -force && ./pok` locally to reproduce.
          branch: chore/pocket-self-update
          delete-branch: true
