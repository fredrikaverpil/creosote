# Code generated by pocket. DO NOT EDIT.
# Run `./pok github-workflows` to regenerate.
# See: https://github.com/fredrikaverpil/pocket

name: pocket-matrix

on:
  push:
    branches: [main, master]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  plan:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.matrix.outputs.matrix }}
    steps:
      - uses: actions/checkout@v4
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: .pocket/go.mod
          cache: false
      - name: Restore cache
        uses: actions/cache/restore@v4
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-pocket-
      - id: matrix
        run: echo "matrix=$(./pok gha-matrix)" >> "$GITHUB_OUTPUT"

  run:
    name: ${{ matrix.task }} (${{ matrix.os }})
    needs: plan
    if: ${{ needs.plan.outputs.matrix != '{"include":[]}' }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: true
      matrix: ${{ fromJson(needs.plan.outputs.matrix) }}
    steps:
      - uses: actions/checkout@v4
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: .pocket/go.mod
          cache: false
      - name: Restore cache
        uses: actions/cache/restore@v4
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-pocket-
      - name: ${{ matrix.task }} (unix)
        if: ${{ !contains(matrix.os, 'windows') }}
        shell: bash
        run: ${{ matrix.shim }} -v ${{ matrix.gitDiff && '-g' || '' }} ${{ matrix.task }}
      - name: ${{ matrix.task }} (windows)
        if: ${{ contains(matrix.os, 'windows') }}
        shell: pwsh
        run: ${{ matrix.shim }} -v ${{ matrix.gitDiff && '-g' || '' }} ${{ matrix.task }}
      - name: Check for uncommitted changes
        if: ${{ matrix.gitDiff }}
        shell: bash
        run: git diff --exit-code
      - name: Save cache
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
        uses: actions/cache/save@v4
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-${{ hashFiles('**/go.sum') }}
