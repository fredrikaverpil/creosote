name: action

on:
  push:
  workflow_dispatch:

jobs:
  action:
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.7", "3.8", "3.9", "3.10"]
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        id: cpython_setup
        with:
          python-version: ${{ matrix.python-version }}
      - uses: actions/cache@v3
        id: cache
        with:
          path: |
            ~/.cache/pip
            ~/.cache/pypoetry/virtualenvs
          key: key1-${{ runner.os }}-${{ runner.arch }}-py-${{ matrix.python-version }}-${{ hashFiles('poetry.lock') }}
      - run: pipx install poetry --python '${{ steps.cpython_setup.outputs.python-path }}'
      - run: poetry install --without dev

      - run: pipx install creosote --python '${{ steps.cpython_setup.outputs.python-path }}'

      - run: creosote
