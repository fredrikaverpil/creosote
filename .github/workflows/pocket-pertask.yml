# Code generated by pocket. DO NOT EDIT.
# Run `./pok github-workflows` to regenerate.
# See: https://github.com/fredrikaverpil/pocket

name: pocket

on:
  push:
    branches: [main, master]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  py-format-3-10-ubuntu:
    name: py-format:3.10 (ubuntu-latest)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: .pocket/go.mod
          cache: false
      - name: Restore cache
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-py-format-3-10-ubuntu-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-pocket-py-format-3-10-ubuntu-
      - name: Run py-format:3.10
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: ./pok -v -g -c py-format:3.10
      - name: Save cache
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
        uses: actions/cache/save@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-py-format-3-10-ubuntu-${{ hashFiles('**/go.sum') }}

  py-lint-3-10-ubuntu:
    name: py-lint:3.10 (ubuntu-latest)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: .pocket/go.mod
          cache: false
      - name: Restore cache
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-py-lint-3-10-ubuntu-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-pocket-py-lint-3-10-ubuntu-
      - name: Run py-lint:3.10
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: ./pok -v -g -c py-lint:3.10
      - name: Save cache
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
        uses: actions/cache/save@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-py-lint-3-10-ubuntu-${{ hashFiles('**/go.sum') }}

  py-typecheck-3-10-ubuntu:
    name: py-typecheck:3.10 (ubuntu-latest)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: .pocket/go.mod
          cache: false
      - name: Restore cache
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-py-typecheck-3-10-ubuntu-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-pocket-py-typecheck-3-10-ubuntu-
      - name: Run py-typecheck:3.10
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: ./pok -v -g -c py-typecheck:3.10
      - name: Save cache
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
        uses: actions/cache/save@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-py-typecheck-3-10-ubuntu-${{ hashFiles('**/go.sum') }}

  py-test-3-10-ubuntu:
    name: py-test:3.10 (ubuntu-latest)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: .pocket/go.mod
          cache: false
      - name: Restore cache
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-py-test-3-10-ubuntu-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-pocket-py-test-3-10-ubuntu-
      - name: Run py-test:3.10
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: ./pok -v -g -c py-test:3.10
      - name: Save cache
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
        uses: actions/cache/save@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-py-test-3-10-ubuntu-${{ hashFiles('**/go.sum') }}

  py-test-3-10-macos:
    name: py-test:3.10 (macos-latest)
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: .pocket/go.mod
          cache: false
      - name: Restore cache
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-py-test-3-10-macos-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-pocket-py-test-3-10-macos-
      - name: Run py-test:3.10
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: ./pok -v -g -c py-test:3.10
      - name: Save cache
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
        uses: actions/cache/save@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-py-test-3-10-macos-${{ hashFiles('**/go.sum') }}

  py-test-3-10-windows:
    name: py-test:3.10 (windows-latest)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: .pocket/go.mod
          cache: false
      - name: Restore cache
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-py-test-3-10-windows-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-pocket-py-test-3-10-windows-
      - name: Run py-test:3.10
        shell: pwsh
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: .\pok.ps1 -v -g -c py-test:3.10
      - name: Save cache
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
        uses: actions/cache/save@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-py-test-3-10-windows-${{ hashFiles('**/go.sum') }}

  py-test-3-11-ubuntu:
    name: py-test:3.11 (ubuntu-latest)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: .pocket/go.mod
          cache: false
      - name: Restore cache
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-py-test-3-11-ubuntu-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-pocket-py-test-3-11-ubuntu-
      - name: Run py-test:3.11
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: ./pok -v -g -c py-test:3.11
      - name: Save cache
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
        uses: actions/cache/save@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-py-test-3-11-ubuntu-${{ hashFiles('**/go.sum') }}

  py-test-3-11-macos:
    name: py-test:3.11 (macos-latest)
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: .pocket/go.mod
          cache: false
      - name: Restore cache
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-py-test-3-11-macos-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-pocket-py-test-3-11-macos-
      - name: Run py-test:3.11
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: ./pok -v -g -c py-test:3.11
      - name: Save cache
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
        uses: actions/cache/save@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-py-test-3-11-macos-${{ hashFiles('**/go.sum') }}

  py-test-3-11-windows:
    name: py-test:3.11 (windows-latest)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: .pocket/go.mod
          cache: false
      - name: Restore cache
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-py-test-3-11-windows-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-pocket-py-test-3-11-windows-
      - name: Run py-test:3.11
        shell: pwsh
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: .\pok.ps1 -v -g -c py-test:3.11
      - name: Save cache
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
        uses: actions/cache/save@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-py-test-3-11-windows-${{ hashFiles('**/go.sum') }}

  py-test-3-12-ubuntu:
    name: py-test:3.12 (ubuntu-latest)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: .pocket/go.mod
          cache: false
      - name: Restore cache
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-py-test-3-12-ubuntu-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-pocket-py-test-3-12-ubuntu-
      - name: Run py-test:3.12
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: ./pok -v -g -c py-test:3.12
      - name: Save cache
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
        uses: actions/cache/save@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-py-test-3-12-ubuntu-${{ hashFiles('**/go.sum') }}

  py-test-3-12-macos:
    name: py-test:3.12 (macos-latest)
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: .pocket/go.mod
          cache: false
      - name: Restore cache
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-py-test-3-12-macos-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-pocket-py-test-3-12-macos-
      - name: Run py-test:3.12
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: ./pok -v -g -c py-test:3.12
      - name: Save cache
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
        uses: actions/cache/save@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-py-test-3-12-macos-${{ hashFiles('**/go.sum') }}

  py-test-3-12-windows:
    name: py-test:3.12 (windows-latest)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: .pocket/go.mod
          cache: false
      - name: Restore cache
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-py-test-3-12-windows-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-pocket-py-test-3-12-windows-
      - name: Run py-test:3.12
        shell: pwsh
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: .\pok.ps1 -v -g -c py-test:3.12
      - name: Save cache
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
        uses: actions/cache/save@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-py-test-3-12-windows-${{ hashFiles('**/go.sum') }}

  py-test-3-13-ubuntu:
    name: py-test:3.13 (ubuntu-latest)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: .pocket/go.mod
          cache: false
      - name: Restore cache
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-py-test-3-13-ubuntu-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-pocket-py-test-3-13-ubuntu-
      - name: Run py-test:3.13
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: ./pok -v -g -c py-test:3.13
      - name: Save cache
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
        uses: actions/cache/save@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-py-test-3-13-ubuntu-${{ hashFiles('**/go.sum') }}

  py-test-3-13-macos:
    name: py-test:3.13 (macos-latest)
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: .pocket/go.mod
          cache: false
      - name: Restore cache
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-py-test-3-13-macos-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-pocket-py-test-3-13-macos-
      - name: Run py-test:3.13
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: ./pok -v -g -c py-test:3.13
      - name: Save cache
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
        uses: actions/cache/save@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-py-test-3-13-macos-${{ hashFiles('**/go.sum') }}

  py-test-3-13-windows:
    name: py-test:3.13 (windows-latest)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: .pocket/go.mod
          cache: false
      - name: Restore cache
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-py-test-3-13-windows-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-pocket-py-test-3-13-windows-
      - name: Run py-test:3.13
        shell: pwsh
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: .\pok.ps1 -v -g -c py-test:3.13
      - name: Save cache
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
        uses: actions/cache/save@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-py-test-3-13-windows-${{ hashFiles('**/go.sum') }}

  py-test-3-14-ubuntu:
    name: py-test:3.14 (ubuntu-latest)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: .pocket/go.mod
          cache: false
      - name: Restore cache
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-py-test-3-14-ubuntu-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-pocket-py-test-3-14-ubuntu-
      - name: Run py-test:3.14
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: ./pok -v -g -c py-test:3.14
      - name: Save cache
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
        uses: actions/cache/save@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-py-test-3-14-ubuntu-${{ hashFiles('**/go.sum') }}

  py-test-3-14-macos:
    name: py-test:3.14 (macos-latest)
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: .pocket/go.mod
          cache: false
      - name: Restore cache
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-py-test-3-14-macos-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-pocket-py-test-3-14-macos-
      - name: Run py-test:3.14
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: ./pok -v -g -c py-test:3.14
      - name: Save cache
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
        uses: actions/cache/save@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-py-test-3-14-macos-${{ hashFiles('**/go.sum') }}

  py-test-3-14-windows:
    name: py-test:3.14 (windows-latest)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: .pocket/go.mod
          cache: false
      - name: Restore cache
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-py-test-3-14-windows-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-pocket-py-test-3-14-windows-
      - name: Run py-test:3.14
        shell: pwsh
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: .\pok.ps1 -v -g -c py-test:3.14
      - name: Save cache
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
        uses: actions/cache/save@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-py-test-3-14-windows-${{ hashFiles('**/go.sum') }}

  creosote-ubuntu:
    name: creosote (ubuntu-latest)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: .pocket/go.mod
          cache: false
      - name: Restore cache
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-creosote-ubuntu-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-pocket-creosote-ubuntu-
      - name: Run creosote
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: ./pok -v -g -c creosote
      - name: Save cache
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
        uses: actions/cache/save@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-creosote-ubuntu-${{ hashFiles('**/go.sum') }}

  pre-commit-check-ubuntu:
    name: pre-commit-check (ubuntu-latest)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: .pocket/go.mod
          cache: false
      - name: Restore cache
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-pre-commit-check-ubuntu-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-pocket-pre-commit-check-ubuntu-
      - name: Run pre-commit-check
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: ./pok -v -g -c pre-commit-check
      - name: Save cache
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
        uses: actions/cache/save@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-pre-commit-check-ubuntu-${{ hashFiles('**/go.sum') }}

  github-workflows-ubuntu:
    name: github-workflows (ubuntu-latest)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: .pocket/go.mod
          cache: false
      - name: Restore cache
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-github-workflows-ubuntu-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-pocket-github-workflows-ubuntu-
      - name: Run github-workflows
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: ./pok -v -g -c github-workflows
      - name: Save cache
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
        uses: actions/cache/save@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-github-workflows-ubuntu-${{ hashFiles('**/go.sum') }}

