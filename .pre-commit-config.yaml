repos:
  - repo: https://github.com/fredrikaverpil/creosote
    rev: v2.7.0
    hooks:
      - id: creosote
        args:
          - --venv=.venv
          - --paths=src
          - --deps-file=pyproject.toml
          - --sections=project.dependencies
#          - --pep582
