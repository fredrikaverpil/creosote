[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
name = "creosote"
version = "0.1.0"
description = "This file is only used for testing with creosote."
authors = []

[tool.poetry.dependencies]
python = ">=3.7, <3.12"
distlib = "^0.3.4"
dotty-dict = "^1.3.1"
loguru = "^0.6.0"
toml = "^0.10.2"

[tool.poetry.group.dev.dependencies]
black = "*"
ruff = "*"
mypy = "*"
pytest = "*"
loguru-mypy = "*"
types-toml = "*"

[tool.poetry.group.directrefs.dependencies]
idna = { git = "https://github.com/kjd/idna.git", branch = "master" }